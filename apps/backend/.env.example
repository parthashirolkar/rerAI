# -------- Backend persistence --------
# Local development can use SQLite:
DATABASE_URI=sqlite:///tmp/rerai-backend.db
# Railway / Supabase should use Postgres:
# DATABASE_URI=postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres?sslmode=require

# Auto-create/check LangGraph tables plus rerAI metadata tables at startup
LANGGRAPH_SETUP_DB=true

# Browser origins allowed to call the public backend directly.
CLIENT_ORIGINS=http://localhost:5173

# Convex deployment URL used for user-scoped backend auth and thread ownership checks.
# Local dev usually uses the Convex cloud URL, not the Convex site URL.
CONVEX_URL=

# -------- Model/provider credentials --------
OPENROUTER_API_KEY=

# -------- MahaRERA optional overrides --------
MAHARERA_PUBLIC_USERNAME=@maharera_public_view
MAHARERA_PUBLIC_PASSWORD=Maharera!@$1
MAHARERA_CRYPTOJS_KEY=sdjhfsdkjgkls74385385

# -------- Chroma Cloud settings --------
CHROMA_API_KEY=
CHROMA_TENANT=c73d822b-82e7-4ba1-bd24-291c352b02f4
CHROMA_DATABASE=RerAI-prod

# Optional local fallback for first-time UDCPR ingestion.
# Relative paths are resolved from the backend working directory.
UDCPR_PDF_DIR=../../data/pdfs
